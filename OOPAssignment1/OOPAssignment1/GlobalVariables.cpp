#include "GlobalVariables.h"
#include "Book.h"

int bookcountwcopies = 0; //bookcount but includes copies too
int bookcount = 0; //doesn't increment when copies of a book are added
int lifetimebookcount = 0;
Book inventory[10000];

